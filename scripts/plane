#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
exec docker compose --project-name "${SWITCHYARD_COMPOSE_PROJECT:-switchyard}" --env-file "${SWITCHYARD_PLANE_ENV:-.env.plane}" -f deploy/docker-compose.yml -f deploy/compose.override.yml "$@"
